import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Coffee Journal")
            .font(.largeTitle)
            .fontWeight(.bold)
    }
}

#Preview {
    ContentView()
}
